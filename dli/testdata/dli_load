EXEC DLI LOAD SEGMENT(CASE) FROM(WS-CASE) END-EXEC

EXEC DLI LOAD USING PCB(DBDA)
     VARIABLE
     SEGMENT(CASE)
     SEGMENT((SEGMENT-NAME))
     SEGLENGTH(LENGTH(SEGMENT-NAME))
     FROM(WS-CASE)
END-EXEC
