SET PATH S1;
SET PATH SYSTEM PATH;
SET PATH SESSION_USER;
SET PATH USER;
SET PATH PATH;
SET PATH CURRENT PATH;
SET PATH CURRENT PACKAGE PATH;
SET PATH :HV1;
SET PATH 'stringConstant';

SET PATH S1, SYSTEM PATH, SESSION_USER, USER, CURRENT PATH, CURRENT PACKAGE PATH, :HV1, 'stringConstant';
SET PATH CURRENT PACKAGE PATH, 'stringConstant', :HV1;

SET CURRENT PATH S1;
SET CURRENT PATH SYSTEM PATH;
SET CURRENT PATH SESSION_USER;
SET CURRENT PATH USER;
SET CURRENT PATH PATH;
SET CURRENT PATH CURRENT PATH;
SET CURRENT PATH CURRENT PACKAGE PATH;
SET CURRENT PATH :HV1;
SET CURRENT PATH 'stringConstant';

SET CURRENT PATH S1, SYSTEM PATH, SESSION_USER, USER, CURRENT PATH, CURRENT PACKAGE PATH, :HV1, 'stringConstant';
SET CURRENT PATH CURRENT PACKAGE PATH, 'stringConstant', :HV1;

SET PATH = S1;
SET PATH = SYSTEM PATH;
SET PATH = SESSION_USER;
SET PATH = USER;
SET PATH = PATH;
SET PATH = CURRENT PATH;
SET PATH = CURRENT PACKAGE PATH;
SET PATH = :HV1;
SET PATH = 'stringConstant';

SET PATH = S1, SYSTEM PATH, SESSION_USER, USER, CURRENT PATH, CURRENT PACKAGE PATH, :HV1, 'stringConstant';
SET PATH = CURRENT PACKAGE PATH, 'stringConstant', :HV1;

SET CURRENT PATH = S1;
SET CURRENT PATH = SYSTEM PATH;
SET CURRENT PATH = SESSION_USER;
SET CURRENT PATH = USER;
SET CURRENT PATH = PATH;
SET CURRENT PATH = CURRENT PATH;
SET CURRENT PATH = CURRENT PACKAGE PATH;
SET CURRENT PATH = :HV1;
SET CURRENT PATH = 'stringConstant';

SET CURRENT PATH = S1, SYSTEM PATH, SESSION_USER, USER, CURRENT PATH, CURRENT PACKAGE PATH, :HV1, 'stringConstant';
SET CURRENT PATH = CURRENT PACKAGE PATH, 'stringConstant', :HV1;


