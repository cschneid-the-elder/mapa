SET CURRENT DEBUG MODE = :WS-MODE;
SET CURRENT DEBUG MODE = DISALLOW;
SET CURRENT DEBUG MODE = ALLOW;
SET CURRENT DEBUG MODE = DISABLE;
SET CURRENT DEBUG MODE = :WS-MODE;
SET CURRENT DEBUG MODE = DISALLOW;
SET CURRENT DEBUG MODE = ALLOW;
SET CURRENT DEBUG MODE = DISABLE;
SET CURRENT 
    DEBUG 
    MODE = DISABLE;

