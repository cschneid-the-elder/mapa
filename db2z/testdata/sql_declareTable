EXEC SQL DECLARE FARSCAPE.BAM TABLE
     (ID        CHAR(6)     NOT NULL,
      FIRST_NM  VARCHAR(12) NOT NULL,
      MID_INTL  CHAR(1)     NOT NULL,
      LAST_NM   VARCHAR(15) NOT NULL,
      DEPT      CHAR(3)             ,
      PHONE_NB  CHAR(4)             ,
      HIREDATE  DATE                ,
      JOB_CODE  CHAR(8)             ,
      EDU_LEVEL SMALLINT            ,
      SEX       CHAR(1)             ,
      BIRTHDATE DATE                ,
      SALARY    DECIMAL(9,2)        ,
      BONUS     DECIMAL(9,2)        ,
      COMM      DECIMAL(9,2)        );

DECLARE RURITANIAN_SALES TABLE
     (PRODUCT_CODE   INTEGER,
      MONTH          INTEGER, -- embedded comment
      YEAR           INTEGER,
      TOTAL          DECIMAL(9,2));


