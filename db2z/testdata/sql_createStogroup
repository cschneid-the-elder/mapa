CREATE STOGROUP SG1 VCAT SGC;
CREATE STOGROUP SG1
    VOLUMES(ONEILL)
    VCAT SGC
;

CREATE STOGROUP SG1
    VOLUMES (ONEILL, CARTER, JACKSON, TEALC)
    VCAT SGC
;

CREATE STOGROUP SG1
    VOLUMES (ONEILL, CARTER, JACKSON, TEALC)
    VCAT SGC
    DATACLAS MITCHELL
;

CREATE STOGROUP SG1
    VOLUMES (ONEILL, CARTER, JACKSON, TEALC)
    VCAT SGC
    MGMTCLAS LANDRY
;

CREATE STOGROUP SG1
    VOLUMES (ONEILL, CARTER, JACKSON, TEALC)
    VCAT SGC
    STORCLAS VALA
;

CREATE STOGROUP SG1
    VOLUMES (ONEILL, CARTER, JACKSON, TEALC)
    VCAT SGC
    DATACLAS MITCHELL
    MGMTCLAS LANDRY
;

CREATE STOGROUP SG1
    VOLUMES (ONEILL, CARTER, JACKSON, TEALC)
    VCAT SGC
    MGMTCLAS LANDRY
    STORCLAS VALA
;

CREATE STOGROUP SG1
    VOLUMES (ONEILL, CARTER, JACKSON, TEALC)
    VCAT SGC
    DATACLAS MITCHELL
    STORCLAS VALA
;

CREATE STOGROUP SG1
    VOLUMES (ONEILL, CARTER, JACKSON, TEALC)
    VCAT SGC
    DATACLAS MITCHELL
    MGMTCLAS LANDRY
    STORCLAS VALA
;

CREATE STOGROUP SG1
    VOLUMES (ONEILL, CARTER, JACKSON, TEALC)
    VCAT SGC
    DATACLAS MITCHELL
    MGMTCLAS LANDRY
    STORCLAS VALA
    NO KEY LABEL
;

CREATE STOGROUP SG1
    VOLUMES (ONEILL, CARTER, JACKSON, TEALC)
    VCAT SGC
    DATACLAS MITCHELL
    MGMTCLAS LANDRY
    STORCLAS VALA
    KEY LABEL QUINN
;


