SELECT
    ARRAY_AGG(C1 ORDER BY PRIORITY)
INTO X
FROM T1
WHERE KEY = :HV1;

SET X = (SELECT ARRAY_AGG(E) FROM UNNEST(AN_ARRAY) AS T(E));

SET NUMBERS =
     (SELECT ARRAY_AGG(C1)
      FROM T1
      WHERE KEY = :HV1);

SELECT
    ARRAY_AGG(KEY, C1)
INTO AN_ARRAY
FROM T1 WHERE C2 = 1;

